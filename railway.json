{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py seed && gunicorn backend.wsgi:application --bind 0.0.0.0:${PORT:-8080}",
    "restartPolicyType": "ON_FAILURE"
  }
}
